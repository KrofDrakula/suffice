name: Tests
on: push

jobs:
  interpreter:
    name: Interpreter
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/setup
      - run: |
          pnpm install
          cd packages/interpreter
          pnpm ci-test
